// TODO: list cases (cr3ed_casesdev rows) via lib/dataverse.ts, scoped to
// the signed-in consulting client's Business Unit.
export default function DashboardPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold">Cases</h1>
      <p className="mt-2 text-slate-500">
        Case list will render here once the Dataverse client is wired up.
      </p>
    </div>
  );
}
